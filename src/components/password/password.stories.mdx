import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Password from './password.tsx';

<Meta title="Components/Password" component={Password} />

export const Template = (args) => <Password {...args} />;

## Password input

<Canvas>
  <Story name="Password">{Template.bind({})}</Story>
  <Story
    name="Password with args"
    args={{
      id: 'password',
      onChange: (value) => {
        console.log(value);
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Password" />
