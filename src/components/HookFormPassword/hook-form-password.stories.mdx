import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import HookFormPassword from './hook-form-password.tsx';

<Meta title="Components/Hook Form Password" component={HookFormPassword} />

export const Template = (args) => <HookFormPassword {...args} />;

## Password input

<Canvas>
  <Story name="Hook Form Password">{Template.bind({})}</Story>
  <Story
    name="Hook Form Password with args"
    args={{
      id: 'password',
      onChange: (value) => {
        console.log(value);
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Hook Form Password" />
